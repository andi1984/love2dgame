[package]
name = "racing-sim"
version = "0.1.0"
edition = "2021"
description = "Pure game logic for the racing game: physics, AI, track generation. No engine dependency."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
